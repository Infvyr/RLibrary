import { Typography } from '@mui/material';

const ResetPasswordHeader = () => {
	return (
		<Typography variant="h1" sx={{ p: 3, fontSize: '3rem' }}>
			RLibrary
		</Typography>
	);
};

export default ResetPasswordHeader;
